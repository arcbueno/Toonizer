# Toonizer
Script to convert JSON to TOON

### To run
1. Update the example.json on /input
2. Run the following commands
    ```bash
    npm i
    ```
    ```bash
    npx tsc -w
    ```
    ```bash
    node dist/index.js
    ```
